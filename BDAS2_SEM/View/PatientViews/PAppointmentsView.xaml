<UserControl x:Class="BDAS2_SEM.View.PatientViews.PAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:BDAS2_SEM.View.PatientViews"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Button Content="Записаться" Command="{Binding BookAppointmentCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock Text="Прошедшие встречи" FontWeight="Bold" Margin="10,20,10,10"/>
            <ListView ItemsSource="{Binding PastAppointments}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Datum}" />
                        <GridViewColumn Header="Время" DisplayMemberBinding="{Binding Cas}" />
                        <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Status}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBlock Text="Будущие встречи" FontWeight="Bold" Margin="10,20,10,10"/>
            <ListView ItemsSource="{Binding FutureAppointments}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Datum}" />
                        <GridViewColumn Header="Время" DisplayMemberBinding="{Binding Cas}" />
                        <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Status}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>