<!-- View/DoctorViews/AppointmentsView.xaml -->
<UserControl x:Class="BDAS2_SEM.View.DoctorViews.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BDAS2_SEM.View.DoctorViews">
    <Grid>
        <DataGrid ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Patient ID" Binding="{Binding PacientId}" />
                <DataGridTextColumn Header="Date" Binding="{Binding Datum, StringFormat={}{0:MM/dd/yyyy}}" />
                <DataGridTextColumn Header="Time" Binding="{Binding Cas, StringFormat={}{0:HH:mm}}" />
                <DataGridTextColumn Header="Room" Binding="{Binding Mistnost}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Accept"
                                        Command="{Binding DataContext.AcceptAppointmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        IsEnabled="{Binding CanAccept}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Cancel"
                                        Command="{Binding DataContext.CancelAppointmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        IsEnabled="{Binding CanCancel}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>